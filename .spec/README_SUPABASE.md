# Supabase移行ドキュメント一覧

このディレクトリには、Supabase（PostgreSQL）への移行に必要なドキュメント、SQL、API仕様が含まれています。

## 📋 ドキュメント構成

### 1. **supabase-setup-guide.md** ⭐ 最初に読む
- Supabaseプロジェクトの初期化手順
- SQL スキーマ適用方法
- Supabase Auth設定（メール認証、URL設定、JWT設定）
- ユーザー管理
- ストレージ設定（画像アップロード）
- Lambda連携設定
- API Gateway連携
- トラブルシューティング

**対象者**: システム管理者、エンジニア

---

### 2. **supabase-schema.sql** - PostgreSQL テーブル定義
包含内容：
- 7つのテーブル定義
  - `users` - ユーザー情報（Supabase Auth連携）
  - `articles` - 記事管理
  - `media` - メディア/画像管理
  - `ai_chat_history` - AIチャット履歴
  - `file_change_history` - ファイル変更履歴
  - `knowledge_documents` - 知識ベース管理
  - `line_conversations` - LINE会話履歴
- インデックス設定
- 自動更新トリガー関数
- テーブルコメント

**使用方法**:
```
1. Supabase ダッシュボード → SQL Editor → New Query
2. supabase-schema.sql の内容をコピー・貼り付け
3. 実行
```

**対象者**: データベース管理者

---

### 3. **supabase-rls-policies.sql** - Row Level Security ポリシー
包含内容：
- 7つのテーブル用 RLS ポリシー
- 各テーブルのアクセス制御ルール
  - `users`: ユーザーは自身のプロフィールのみ参照
  - `articles`: 公開済み記事は全員参照可、下書きは作成者/admin のみ
  - `media`: 全員が参照、admin のみ削除
  - `ai_chat_history`: ユーザーは自身のセッションのみ参照
  - `file_change_history`: admin のみアクセス
  - `knowledge_documents`: admin のみアクセス
  - `line_conversations`: システム内部のみアクセス

**使用方法**:
```
1. Supabase ダッシュボード → SQL Editor → New Query
2. supabase-rls-policies.sql の内容をコピー・貼り付け
3. 実行
```

**セキュリティ重要度**: ⚠️ **重要** - データアクセス制御の全てを担当

**対象者**: セキュリティエンジニア、データベース管理者

---

### 4. **supabase-api-design.md** - REST API 仕様書
包含内容：
- API 全体の概要（認証、レスポンス形式）
- 9つのエンドポイントカテゴリ
  1. 認証API（signup, login, logout, refresh, me）
  2. 記事管理API（CRUD操作）
  3. メディア/画像API（アップロード、一覧、削除）
  4. 検索API（全文検索）
  5. LINE連携API（Webhook、配信）
  6. ユーザー管理API（admin機能）
  7. Dify連携API（知識ベース）
- 各エンドポイントの詳細：
  - Request/Response 仕様
  - Query Parameters
  - Authorization 要件
  - エラーコード
- Lambda実装例（Node.js, Python）

**使用方法**:
```
1. Lambda関数開発時に参照
2. API仕様に従い実装
3. テストコード作成
```

**対象者**: バックエンドエンジニア

---

### 5. **supabase-checklist.md** - セットアップ完了チェックリスト
包含内容：
- 10フェーズの詳細チェックリスト
  1. Supabaseプロジェクト作成
  2. Supabase Auth設定
  3. 初期ユーザー作成
  4. ストレージ設定
  5. AWS Lambda環境設定
  6. API Gateway設定
  7. 管理画面フロントエンド連携
  8. テスト実行
  9. 本番環境デプロイ
  10. 運用・保守設定
- トラブルシューティング
- 完了日時記録

**使用方法**:
```
1. 各項目をチェックしながら実施
2. 完了したら ✓ を付ける
3. トラブルが発生したら該当セクションを確認
```

**対象者**: プロジェクト管理者、実施者

---

## 🔄 推奨実施順序

### Day 1: Supabaseプロジェクト初期化
1. `supabase-setup-guide.md` の「1. Supabaseプロジェクト初期化」を読む
2. Supabaseアカウント作成 → プロジェクト作成
3. プロジェクト情報をコピー・保管
4. `supabase-schema.sql` を実行
5. `supabase-rls-policies.sql` を実行

### Day 2: 認証・ユーザー管理設定
1. `supabase-setup-guide.md` の「2-3. Supabase Auth設定」を読む
2. メール認証設定
3. URL/JWT設定
4. 管理者ユーザー作成
5. editor ユーザー作成

### Day 3: ストレージ・Lambda連携
1. `supabase-setup-guide.md` の「4-5. ストレージ・Lambda設定」を読む
2. ストレージバケット作成
3. RLS ポリシー設定
4. Lambda 環境変数設定
5. Lambda 関数テスト

### Day 4-5: API実装
1. `supabase-api-design.md` で API仕様確認
2. Lambda 関数実装（認証、記事CRUD、メディアアップロード等）
3. API Gateway 設定
4. 統合テスト実行

### Day 6: テスト・デプロイ
1. `supabase-checklist.md` でテストケース実行
2. RLS ポリシーテスト
3. セキュリティレビュー
4. 本番環境デプロイ

---

## 📊 テーブル構成図

```
┌─────────────────────────────────────────┐
│          Supabase PostgreSQL             │
├─────────────────────────────────────────┤
│                                          │
│  users (users)                          │
│  ├─ id (UUID)                           │
│  ├─ email (VARCHAR)                     │
│  ├─ name (VARCHAR)                      │
│  ├─ role (admin/editor)                 │
│  └─ created_at (TIMESTAMP)              │
│          ↓                               │
│  articles (記事)                         │
│  ├─ id (UUID)                           │
│  ├─ title, content, category            │
│  ├─ author (FK → users)                 │
│  ├─ status (published/draft)            │
│  ├─ line_published, x_published         │
│  └─ created_at, published_at            │
│          ↓                               │
│  media (画像/メディア)                    │
│  ├─ id (UUID)                           │
│  ├─ file_name, file_url, file_size      │
│  ├─ uploaded_by (FK → users)            │
│  └─ created_at                          │
│                                          │
│  ai_chat_history (AIチャット)            │
│  ├─ id (UUID)                           │
│  ├─ session_id (VARCHAR)                │
│  ├─ user_id (FK → users)                │
│  ├─ content                             │
│  └─ created_at                          │
│                                          │
│  file_change_history (ファイル変更)      │
│  ├─ id (UUID)                           │
│  ├─ file_path, change_type              │
│  ├─ old_content, new_content            │
│  ├─ status (pending/approved/rejected)  │
│  └─ created_at, approved_at             │
│                                          │
│  knowledge_documents (知識ベース)         │
│  ├─ id (UUID)                           │
│  ├─ file_name, file_type                │
│  ├─ dify_document_id, dify_dataset_id   │
│  ├─ status (processing/completed)       │
│  └─ created_at, indexed_at              │
│                                          │
│  line_conversations (LINE会話)           │
│  ├─ id (UUID)                           │
│  ├─ line_user_id (VARCHAR)              │
│  ├─ content, dify_conversation_id       │
│  ├─ response_time_ms                    │
│  └─ created_at                          │
│                                          │
└─────────────────────────────────────────┘
```

---

## 🔐 セキュリティチェックポイント

### RLS (Row Level Security)
- ✅ `users`: ユーザーは自身のデータのみアクセス
- ✅ `articles`: 公開済み記事は全員参照、下書きは制限
- ✅ `media`: 認証ユーザーが管理、admin のみ削除
- ✅ `file_change_history`: admin のみ
- ✅ `knowledge_documents`: admin のみ
- ✅ `line_conversations`: システム内部のみ

### 認証
- ✅ Supabase Auth で JWT管理
- ✅ アクセストークン: 1時間有効
- ✅ リフレッシュトークン: 7日間有効
- ✅ パスワード: bcrypt でハッシュ化

### ストレージ
- ✅ S3/Supabase Storage でHTTP経由アクセス
- ✅ HTTPS/TLS暗号化
- ✅ ファイルアップロード検証（サイズ、タイプ）

---

## 🛠️ トラブルシューティング

**Q: RLS エラーが出る**
A: `supabase-setup-guide.md` → 8.1 を参照

**Q: JWT トークンエラー**
A: `supabase-setup-guide.md` → 8.2 を参照

**Q: ストレージアップロードエラー**
A: `supabase-setup-guide.md` → 8.3 を参照

---

## 📞 サポート

### リソース
- Supabase Official Docs: https://supabase.io/docs
- PostgreSQL Docs: https://www.postgresql.org/docs/
- Supabase Discord: https://discord.supabase.io

### 質問テンプレート
```
エラーメッセージ:
[ここにエラーメッセージを記入]

実行したコマンド/操作:
[ここに詳細を記入]

期待される動作:
[ここに期待値を記入]

該当ドキュメント:
[参照したドキュメント名]
```

---

**最終更新**: 2025年11月13日
**バージョン**: 1.0
**ステータス**: ✅ 仕様確定

---

## 次のステップ

1. **[緊急]** Supabaseプロジェクトを東京リージョンで作成
2. **[重要]** `supabase-schema.sql` と `supabase-rls-policies.sql` を実行
3. **[重要]** Supabase Auth設定完了
4. **[重要]** Lambda関数実装開始（`supabase-api-design.md` 参照）
5. **[テスト]** `supabase-checklist.md` で全テスト実行
6. **[本番]** 本番環境へデプロイ

質問があればお気軽にお声かけください！
